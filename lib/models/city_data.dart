/// Data class for city information on the map
class CityData {
  final String name;
  final double xPercent;
  final double yPercent;
  final String mapFile;

  const CityData(this.name, this.xPercent, this.yPercent, this.mapFile);
}
